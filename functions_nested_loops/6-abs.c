#include "main.h"
#include <stdlib.h>
/**
 * _abs -> function
 * @x: parameter int
 * Calculate the absolute value of a whole number
 * Return: x (success);
*/

int _abs(int x)
{
	x = abs(x);
	return (x);
}
